{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "yarn lerna run dev --parallel",
    "build": "yarn lerna run build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.4.1"
  }
}
